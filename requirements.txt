# App runtime: pip (Streamlit Community Cloud, venv, CI). See local_env/ for Conda.
# Linux/Streamlit: CPU wheel from PyTorch index (avoids multi‑GB CUDA packages). macOS/Win: default PyPI.
--extra-index-url https://download.pytorch.org/whl/cpu
# torchvision 0.24.0+cpu pins torch==2.9.0 (not 2.9.1) — must match.
torch==2.9.0+cpu; platform_system == "Linux"
torch==2.4.1; platform_system != "Linux"
torchvision==0.24.0+cpu; platform_system == "Linux"
torchvision==0.19.1; platform_system != "Linux"
python-dotenv
streamlit
pandas
numpy>=1.26,<3
pyarrow
scikit-learn
rank-bm25
sentence-transformers
faiss-cpu
langchain
langchain-core
langchain-groq
